# Build output: what `npm run build` (Vite) writes and wrangler deploys.
dist/

# Local wrangler dev cache/state — not part of the site.
.wrangler/

# Dev dependency tree (Vite, jsdom). The site itself has no runtime dependencies.
node_modules/
