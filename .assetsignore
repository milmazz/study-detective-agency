# Files here are excluded from what gets uploaded/served by
# `wrangler deploy` and watched by `wrangler dev` — repo/tooling files
# that aren't part of the actual site.
# https://developers.cloudflare.com/workers/static-assets/#ignoring-assets
.git
.gitignore
.wrangler
mise.toml
wrangler.jsonc
README.md
LICENSE
scripts
test
